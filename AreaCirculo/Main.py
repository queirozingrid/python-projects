# programa que calcula a área do círculo após receber, pelo usuário, o valor do raio
pi = float(3.14159)
raio = float(input("Entre com o valor do raio\n"))
area = float(pi*raio*raio)
print(round(area, 2))